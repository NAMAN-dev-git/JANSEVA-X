import { ApplicationStatus } from "@prisma/client";
import { describe, expect, it } from "vitest";
import { assertEmployeeTransition } from "../src/services/application-status.service";
describe("employee status transitions", () => { it("permits only human review decisions", () => { expect(() => assertEmployeeTransition(ApplicationStatus.SUBMITTED, ApplicationStatus.UNDER_REVIEW)).not.toThrow(); expect(() => assertEmployeeTransition(ApplicationStatus.UNDER_REVIEW, ApplicationStatus.APPROVED)).not.toThrow(); expect(() => assertEmployeeTransition(ApplicationStatus.UNDER_REVIEW, ApplicationStatus.CORRECTION_REQUIRED)).not.toThrow(); expect(() => assertEmployeeTransition(ApplicationStatus.UNDER_REVIEW, ApplicationStatus.REJECTED)).not.toThrow(); expect(() => assertEmployeeTransition(ApplicationStatus.APPROVED, ApplicationStatus.SIGNED)).toThrow(); expect(() => assertEmployeeTransition(ApplicationStatus.SIGNED, ApplicationStatus.COMPLETED)).toThrow(); expect(() => assertEmployeeTransition(ApplicationStatus.IDENTITY_VERIFIED, ApplicationStatus.UNDER_REVIEW)).toThrow(); }); });
